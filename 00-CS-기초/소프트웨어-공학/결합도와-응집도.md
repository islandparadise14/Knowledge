---
tags: [CS, 소프트웨어-공학, 결합도, 응집도, 모듈화, Android]
---

# 결합도와 응집도

## 💡 핵심 개념

**결합도(Coupling)**는 모듈 간의 의존 정도를 나타내고, **응집도(Cohesion)**는 모듈 내부 요소들이 얼마나 밀접하게 관련되어 있는지를 나타낸다. 좋은 설계는 **낮은 결합도 + 높은 응집도**를 추구한다.

## 📌 왜 필요한가?

Android 앱의 모듈(feature, data, domain)을 설계할 때 결합도가 높으면 하나의 변경이 여러 모듈에 영향을 주어 유지보수가 어려워진다. 응집도가 높아야 모듈의 목적이 명확하고 재사용성이 높아진다.

## 🔍 자세히

### 결합도 (Coupling)

결합도가 낮을수록 좋다. 느슨한 결합에서 단단한 결합 순서로 정리하면:

| 수준 | 설명 | 예시 |
|------|------|------|
| 메시지 결합 | 인터페이스를 통한 통신 | Repository 인터페이스 |
| 데이터 결합 | 필요한 데이터만 전달 | 함수 파라미터 |
| 스탬프 결합 | 데이터 구조체 전달 | DTO 객체 전달 |
| 제어 결합 | 동작을 제어하는 플래그 전달 | Boolean 파라미터로 분기 |
| 내용 결합 | 다른 모듈 내부에 직접 접근 | 전역 변수 직접 수정 |

#### 높은 결합도 (나쁜 예)

```kotlin
// ❌ ViewModel이 Retrofit 구현에 직접 의존
class UserViewModel : ViewModel() {
    private val retrofit = Retrofit.Builder()
        .baseUrl("https://api.example.com/")
        .build()
    private val api = retrofit.create(UserApi::class.java)

    fun loadUser() {
        viewModelScope.launch {
            val user = api.getUser("123")  // 구체 구현에 강하게 결합
        }
    }
}
```

#### 낮은 결합도 (좋은 예)

```kotlin
// ✅ 인터페이스를 통한 느슨한 결합
interface UserRepository {
    suspend fun getUser(id: String): Result<User>
}

class UserViewModel(
    private val repository: UserRepository  // 추상화에 의존
) : ViewModel() {
    fun loadUser(id: String) {
        viewModelScope.launch {
            repository.getUser(id)
        }
    }
}
```

### 응집도 (Cohesion)

응집도가 높을수록 좋다. 높은 응집도에서 낮은 응집도 순서:

| 수준 | 설명 | 예시 |
|------|------|------|
| 기능적 응집 | 단일 기능 수행 | `EmailValidator` |
| 순차적 응집 | 출력이 다음의 입력 | 데이터 파이프라인 |
| 교환적 응집 | 같은 데이터를 다룸 | `UserRepository` |
| 논리적 응집 | 유사한 성격으로 그룹 | `StringUtils` |
| 우연적 응집 | 관련 없는 기능 모음 | `Utils` 클래스 |

#### 낮은 응집도 (나쁜 예)

```kotlin
// ❌ 관련 없는 기능들이 한 클래스에 모여 있음
object AppUtils {
    fun formatDate(date: Long): String = /* ... */
    fun isNetworkAvailable(context: Context): Boolean = /* ... */
    fun calculateDistance(lat1: Double, lon1: Double, lat2: Double, lon2: Double): Double = /* ... */
    fun compressImage(bitmap: Bitmap): ByteArray = /* ... */
}
```

#### 높은 응집도 (좋은 예)

```kotlin
// ✅ 관련된 기능끼리 모아서 높은 응집도
class DateFormatter(private val locale: Locale = Locale.KOREA) {
    fun formatRelative(date: LocalDateTime): String = /* ... */
    fun formatFull(date: LocalDateTime): String = /* ... */
    fun parse(dateString: String): LocalDateTime = /* ... */
}

class NetworkMonitor(private val context: Context) {
    fun isAvailable(): Boolean = /* ... */
    fun observeConnectivity(): Flow<Boolean> = /* ... */
}

class ImageCompressor(private val quality: Int = 80) {
    fun compress(bitmap: Bitmap): ByteArray = /* ... */
    fun compressToFile(bitmap: Bitmap, file: File): File = /* ... */
}
```

### Android 멀티모듈과 결합도/응집도

```
app/
├── feature/
│   ├── home/       ← 높은 응집도: 홈 화면 관련만
│   ├── profile/    ← 높은 응집도: 프로필 관련만
│   └── settings/
├── domain/         ← UseCase: 비즈니스 로직만
├── data/           ← Repository 구현, API, DB
└── core/
    ├── ui/         ← 공통 UI 컴포넌트
    └── network/    ← 네트워크 설정
```

모듈 간에는 **인터페이스(domain 계층)**를 통해 통신하여 결합도를 낮추고, 각 모듈은 관련된 기능만 포함하여 응집도를 높인다.

```kotlin
// domain 모듈 - 인터페이스 정의
interface PostRepository {
    suspend fun getPosts(): List<Post>
}

// data 모듈 - 구현
class PostRepositoryImpl @Inject constructor(
    private val api: PostApi,
    private val dao: PostDao
) : PostRepository {
    override suspend fun getPosts(): List<Post> = /* ... */
}

// feature 모듈 - domain 인터페이스에만 의존
@HiltViewModel
class HomeViewModel @Inject constructor(
    private val repository: PostRepository
) : ViewModel()
```

## 🔗 관련 개념

- [[00-CS-기초/소프트웨어-공학/SOLID-원칙|SOLID 원칙]]
- [[00-CS-기초/소프트웨어-공학/의존성-주입-개념|의존성 주입]]
- [[00-CS-기초/소프트웨어-공학/OOP-객체지향|OOP 객체지향]]
- [[00-CS-기초/소프트웨어-공학/클린-코드|클린 코드]]

## 📚 더 보기

- [Android Developers - Modularization](https://developer.android.com/topic/modularization)
- [Wikipedia - Coupling](https://en.wikipedia.org/wiki/Coupling_(computer_programming))
- [Wikipedia - Cohesion](https://en.wikipedia.org/wiki/Cohesion_(computer_science))

---

**핵심 요약:** 좋은 Android 설계는 모듈 간 결합도를 낮추고(인터페이스 기반), 모듈 내 응집도를 높여(관련 기능만 포함) 변경에 강하고 테스트하기 쉬운 구조를 만든다.
