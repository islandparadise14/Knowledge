---
tags: [CS, 소프트웨어-공학, 클린코드, 코드품질, Android, Kotlin]
---

# 클린 코드

## 💡 핵심 개념

클린 코드는 **읽기 쉽고, 이해하기 쉽고, 수정하기 쉬운 코드**를 말한다. Robert C. Martin의 "Clean Code" 원칙을 기반으로, 의미 있는 이름, 작은 함수, 명확한 구조를 통해 코드의 품질을 높이는 것이 목표이다.

## 📌 왜 필요한가?

Android 프로젝트는 여러 개발자가 장기간 유지보수한다. 클린 코드를 작성하면 새로운 팀원의 온보딩 시간이 줄고, 버그 발견과 수정이 빨라지며, 기능 추가가 안전해진다.

## 🔍 자세히

### 1. 의미 있는 이름 (Meaningful Names)

```kotlin
// ❌ 나쁜 이름
val d: Int = 0          // 무엇인지 알 수 없음
fun calc(a: Int): Int   // 무엇을 계산하는지?
val list1: List<User>   // 어떤 리스트?

// ✅ 좋은 이름
val elapsedDays: Int = 0
fun calculateShippingFee(distanceKm: Int): Int
val activeUsers: List<User>
```

#### Android 네이밍 컨벤션

```kotlin
// ViewModel의 StateFlow
private val _uiState = MutableStateFlow(HomeUiState())
val uiState: StateFlow<HomeUiState> = _uiState.asStateFlow()

// 이벤트 처리 함수: on + 동사
fun onLoginButtonClick() { /* ... */ }
fun onSearchQueryChanged(query: String) { /* ... */ }

// Boolean 변수: is/has/should 접두사
val isLoading: Boolean
val hasNetworkConnection: Boolean
val shouldShowOnboarding: Boolean

// 리소스 네이밍
// layout: <type>_<module>_<description>
// fragment_home_header.xml
// item_user_list.xml

// id: <type><Module><Description> (camelCase)
// tvUserName, btnSubmit, rvPostList
```

### 2. 함수 (Functions)

함수는 **하나의 일만** 해야 하며, **짧아야** 한다.

```kotlin
// ❌ 나쁜 예: 여러 가지 일을 하는 함수
fun handleUserData(userId: String) {
    val response = api.getUser(userId)
    if (response.isSuccessful) {
        val user = response.body()!!
        val formattedName = "${user.firstName} ${user.lastName}"
        database.insert(user.copy(displayName = formattedName))
        analytics.track("user_loaded")
        _uiState.value = UiState.Success(user)
    } else {
        _uiState.value = UiState.Error("Failed")
        crashlytics.log("API error: ${response.code()}")
    }
}

// ✅ 좋은 예: 작고 명확한 함수들로 분리
fun loadUser(userId: String) {
    viewModelScope.launch {
        _uiState.value = UiState.Loading
        repository.getUser(userId)
            .onSuccess { handleUserLoaded(it) }
            .onFailure { handleUserError(it) }
    }
}

private fun handleUserLoaded(user: User) {
    _uiState.value = UiState.Success(user)
    analytics.trackUserLoaded()
}

private fun handleUserError(error: Throwable) {
    _uiState.value = UiState.Error(error.toUserMessage())
    logger.error("User load failed", error)
}
```

### 3. 주석 (Comments)

코드 자체가 설명이 되어야 한다. 주석은 **왜(Why)**를 설명할 때만 사용한다.

```kotlin
// ❌ 불필요한 주석
// 사용자를 가져온다
fun getUser(id: String): User

// ✅ 필요한 주석: 이유를 설명
// API v2에서 페이지네이션 방식이 변경되어 offset 대신 cursor 사용
// 관련 이슈: https://github.com/example/issues/342
fun fetchPosts(cursor: String?): PostPage

// ✅ TODO 주석
// TODO: Room Migration 3→4 시 이 컬럼 제거 필요
val legacyField: String = ""
```

### 4. 패키지 구조

```
// ✅ Feature 기반 패키지 구조 (권장)
com.example.app/
├── feature/
│   ├── home/
│   │   ├── HomeScreen.kt
│   │   ├── HomeViewModel.kt
│   │   ├── HomeUiState.kt
│   │   └── components/
│   │       ├── PostCard.kt
│   │       └── StoryBar.kt
│   └── profile/
│       ├── ProfileScreen.kt
│       └── ProfileViewModel.kt
├── data/
│   ├── repository/
│   ├── remote/
│   └── local/
├── domain/
│   ├── model/
│   └── usecase/
└── core/
    ├── di/
    ├── ui/
    └── util/
```

### 5. 에러 처리

```kotlin
// ❌ 나쁜 예: 예외 무시
try {
    val user = api.getUser(id)
} catch (e: Exception) {
    // 아무 처리 없음
}

// ✅ 좋은 예: Result 타입 활용
suspend fun getUser(id: String): Result<User> = runCatching {
    api.getUser(id).toDomain()
}.onFailure { error ->
    logger.error("Failed to fetch user: $id", error)
}

// ✅ sealed class로 명시적 에러 처리
sealed class NetworkResult<out T> {
    data class Success<T>(val data: T) : NetworkResult<T>()
    data class Error(val code: Int, val message: String) : NetworkResult<Nothing>()
    data class Exception(val throwable: Throwable) : NetworkResult<Nothing>()
}
```

### 6. Kotlin 관용구 활용

```kotlin
// ❌ Java 스타일
var name: String? = null
if (name != null) {
    println(name.length)
}

val list = mutableListOf<String>()
for (item in items) {
    if (item.isActive) {
        list.add(item.name)
    }
}

// ✅ Kotlin 관용구
name?.let { println(it.length) }

val list = items
    .filter { it.isActive }
    .map { it.name }
```

### 7. 매직 넘버 제거

```kotlin
// ❌ 매직 넘버
if (password.length < 8) { /* ... */ }
delay(3000)

// ✅ 상수로 의미 부여
companion object {
    private const val MIN_PASSWORD_LENGTH = 8
    private const val SPLASH_DELAY_MS = 3_000L
}

if (password.length < MIN_PASSWORD_LENGTH) { /* ... */ }
delay(SPLASH_DELAY_MS)
```

## 🔗 관련 개념

- [[00-CS-기초/소프트웨어-공학/SOLID-원칙|SOLID 원칙]]
- [[00-CS-기초/소프트웨어-공학/결합도와-응집도|결합도와 응집도]]
- [[00-CS-기초/소프트웨어-공학/OOP-객체지향|OOP 객체지향]]
- [[00-CS-기초/소프트웨어-공학/함수형-프로그래밍-개념|함수형 프로그래밍]]

## 📚 더 보기

- [Kotlin 공식 - Coding Conventions](https://kotlinlang.org/docs/coding-conventions.html)
- [Android Kotlin Style Guide](https://developer.android.com/kotlin/style-guide)
- [Clean Code by Robert C. Martin](https://www.oreilly.com/library/view/clean-code/9780136083238/)

---

**핵심 요약:** 클린 코드는 의미 있는 이름, 단일 책임 함수, 명확한 에러 처리, Feature 기반 패키지 구조를 통해 Android 프로젝트의 가독성과 유지보수성을 높인다.
