---
tags: [cs, algorithm, search, binary-search, bfs, dfs]
---

# 탐색 알고리즘

## 💡 핵심 개념

탐색 알고리즘은 데이터 집합에서 특정 값이나 조건을 만족하는 요소를 찾는 알고리즘이다. **선형 탐색**(O(n))과 **이진 탐색**(O(log n))이 기본이며, 그래프/트리에서는 **BFS**와 **DFS**가 사용된다.

## 📌 왜 필요한가?

검색 기능 구현, View 트리 순회, Navigation 그래프 경로 탐색, SQLite 인덱스 탐색 등 Android 앱 전반에 걸쳐 사용된다.

## 🔍 자세히

### 선형 탐색 (Linear Search)

```kotlin
// O(n) - 정렬되지 않은 데이터
fun linearSearch(list: List<Int>, target: Int): Int {
    for (i in list.indices) {
        if (list[i] == target) return i
    }
    return -1
}

// Kotlin 함수 활용
val index = listOf(3, 7, 1, 9).indexOf(9)      // 3
val found = listOf(3, 7, 1, 9).find { it > 5 } // 7
val any = listOf(3, 7, 1, 9).any { it > 8 }    // true
```

### 이진 탐색 (Binary Search)

```kotlin
// O(log n) - 반드시 정렬된 데이터
fun binarySearch(sorted: List<Int>, target: Int): Int {
    var low = 0; var high = sorted.lastIndex
    while (low <= high) {
        val mid = low + (high - low) / 2  // 오버플로우 방지
        when {
            sorted[mid] == target -> return mid
            sorted[mid] < target -> low = mid + 1
            else -> high = mid - 1
        }
    }
    return -1
}

// Kotlin 표준 라이브러리
val sorted = listOf(1, 3, 5, 7, 9)
sorted.binarySearch(5)  // 2
sorted.binarySearch(4)  // 음수 (삽입 위치의 반전)
```

### BFS (너비 우선 탐색)

```kotlin
// O(V + E) - 최단 경로, 레벨 순회
fun bfs(graph: Map<Int, List<Int>>, start: Int): List<Int> {
    val visited = mutableSetOf(start)
    val queue = ArrayDeque<Int>()
    val result = mutableListOf<Int>()

    queue.addLast(start)
    while (queue.isNotEmpty()) {
        val node = queue.removeFirst()
        result.add(node)
        graph[node]?.forEach { neighbor ->
            if (neighbor !in visited) {
                visited.add(neighbor)
                queue.addLast(neighbor)
            }
        }
    }
    return result
}
```

### DFS (깊이 우선 탐색)

```kotlin
// O(V + E) - 경로 탐색, 사이클 감지
fun dfs(graph: Map<Int, List<Int>>, start: Int): List<Int> {
    val visited = mutableSetOf<Int>()
    val result = mutableListOf<Int>()

    fun traverse(node: Int) {
        visited.add(node)
        result.add(node)
        graph[node]?.forEach { if (it !in visited) traverse(it) }
    }

    traverse(start)
    return result
}

// 스택 기반 반복 DFS (StackOverflow 방지)
fun dfsIterative(graph: Map<Int, List<Int>>, start: Int): List<Int> {
    val visited = mutableSetOf<Int>()
    val stack = ArrayDeque<Int>()
    val result = mutableListOf<Int>()

    stack.addLast(start)
    while (stack.isNotEmpty()) {
        val node = stack.removeLast()
        if (node !in visited) {
            visited.add(node)
            result.add(node)
            graph[node]?.reversed()?.forEach { stack.addLast(it) }
        }
    }
    return result
}
```

### BFS vs DFS 선택

| 상황 | 추천 | 이유 |
|---|---|---|
| 최단 경로 | BFS | 레벨 단위 탐색 |
| 모든 경로 탐색 | DFS | 메모리 효율 |
| 트리 레벨 순회 | BFS | 레벨별 처리 |
| View 트리 순회 | DFS | Android View 구조와 일치 |

### Android 실무 활용

```kotlin
// 1. View 트리 순회 (DFS)
fun findAllTextViews(root: ViewGroup): List<TextView> {
    val result = mutableListOf<TextView>()
    for (i in 0 until root.childCount) {
        val child = root.getChildAt(i)
        if (child is TextView) result.add(child)
        if (child is ViewGroup) result.addAll(findAllTextViews(child))
    }
    return result
}

// 2. 정렬된 목록에서 이진 탐색
class SearchViewModel : ViewModel() {
    private val sortedProducts: List<Product> = /* 이름순 정렬 */

    fun search(name: String): Product? {
        val idx = sortedProducts.binarySearchBy(name) { it.name }
        return if (idx >= 0) sortedProducts[idx] else null
    }
}

// 3. Navigation Graph - 내부적으로 BFS 기반 경로 탐색
// NavController.findDestination()이 그래프를 탐색
```

## 🔗 관련 개념

- [[00-CS-기초/알고리즘/시간-복잡도-Big-O|시간 복잡도 Big-O]]
- [[00-CS-기초/알고리즘/정렬-알고리즘|정렬 알고리즘]]
- [[00-CS-기초/자료구조/그래프|그래프]]
- [[00-CS-기초/자료구조/트리|트리]]
- [[00-CS-기초/알고리즘/재귀와-반복|재귀와 반복]]

## 📚 더 보기

- [Kotlin binarySearch](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin.collections/binary-search.html)

---

**핵심 요약:** 정렬된 데이터에는 이진 탐색(O(log n)), 그래프/트리에는 BFS(최단 경로)나 DFS(깊은 탐색). Android의 View 트리는 DFS로 순회된다.
