---
tags: [CS, 자료구조, 그래프, BFS, DFS, 인접리스트, 인접행렬]
---

# 그래프

## 💡 핵심 개념

그래프(Graph)는 **정점(Vertex)**과 **간선(Edge)**으로 이루어진 자료구조로, 노드 간의 다대다 관계를 표현한다. 트리와 달리 순환(Cycle)이 가능하며, 간선에 방향이 있으면 방향 그래프(Directed), 없으면 무방향 그래프(Undirected)이다.

## 📌 왜 필요한가?

Android의 Navigation Graph가 화면 간 이동을 그래프로 표현하며, Dagger/Hilt의 의존성 그래프, Gradle의 태스크 의존성도 그래프 구조이다. SNS의 팔로우 관계, 지도 네비게이션 등 다양한 실무 문제가 그래프로 모델링된다.

## 🔍 자세히

### 그래프 표현 방식

#### 1. 인접 리스트 (Adjacency List) - 실무에서 주로 사용

```kotlin
// Map 기반 인접 리스트
val graph = mapOf(
    "A" to listOf("B", "C"),
    "B" to listOf("A", "D"),
    "C" to listOf("A", "D"),
    "D" to listOf("B", "C")
)

// 가중치 그래프
data class Edge(val to: String, val weight: Int)
val weightedGraph = mapOf(
    "서울" to listOf(Edge("대전", 140), Edge("원주", 100)),
    "대전" to listOf(Edge("서울", 140), Edge("대구", 120)),
    "원주" to listOf(Edge("서울", 100))
)
```

#### 2. 인접 행렬 (Adjacency Matrix)

```kotlin
// 정점 수가 적고 간선이 많을 때 유리
val n = 4 // 정점 수
val matrix = Array(n) { IntArray(n) }
matrix[0][1] = 1 // A → B
matrix[0][2] = 1 // A → C
matrix[1][3] = 1 // B → D
```

### 인접 리스트 vs 인접 행렬

| 특성 | 인접 리스트 | 인접 행렬 |
|------|-----------|-----------|
| 공간 복잡도 | O(V + E) | O(V²) |
| 간선 존재 확인 | O(degree) | **O(1)** |
| 인접 노드 순회 | **O(degree)** | O(V) |
| 적합한 경우 | 희소 그래프 | 밀집 그래프 |

### DFS (깊이 우선 탐색)

```kotlin
fun dfs(graph: Map<String, List<String>>, start: String): List<String> {
    val visited = mutableSetOf<String>()
    val result = mutableListOf<String>()

    fun dfsRecursive(node: String) {
        if (node in visited) return
        visited.add(node)
        result.add(node)
        graph[node]?.forEach { neighbor ->
            dfsRecursive(neighbor)
        }
    }

    dfsRecursive(start)
    return result
}

// 스택 기반 반복적 DFS
fun dfsIterative(graph: Map<String, List<String>>, start: String): List<String> {
    val visited = mutableSetOf<String>()
    val stack = ArrayDeque<String>()
    val result = mutableListOf<String>()

    stack.addLast(start)
    while (stack.isNotEmpty()) {
        val node = stack.removeLast()
        if (node in visited) continue
        visited.add(node)
        result.add(node)
        graph[node]?.reversed()?.forEach { neighbor ->
            if (neighbor !in visited) stack.addLast(neighbor)
        }
    }
    return result
}
```

### BFS (너비 우선 탐색)

```kotlin
fun bfs(graph: Map<String, List<String>>, start: String): List<String> {
    val visited = mutableSetOf<String>()
    val queue = ArrayDeque<String>()
    val result = mutableListOf<String>()

    queue.addLast(start)
    visited.add(start)

    while (queue.isNotEmpty()) {
        val node = queue.removeFirst()
        result.add(node)
        graph[node]?.forEach { neighbor ->
            if (neighbor !in visited) {
                visited.add(neighbor)
                queue.addLast(neighbor)
            }
        }
    }
    return result
}

// 최단 경로 (가중치 없는 그래프)
fun shortestPath(
    graph: Map<String, List<String>>,
    start: String,
    end: String
): List<String>? {
    val visited = mutableSetOf(start)
    val queue = ArrayDeque<List<String>>()
    queue.addLast(listOf(start))

    while (queue.isNotEmpty()) {
        val path = queue.removeFirst()
        val node = path.last()
        if (node == end) return path

        graph[node]?.forEach { neighbor ->
            if (neighbor !in visited) {
                visited.add(neighbor)
                queue.addLast(path + neighbor)
            }
        }
    }
    return null
}
```

### 시간 복잡도

| 알고리즘 | 시간 복잡도 | 공간 복잡도 | 용도 |
|---------|-----------|-----------|------|
| DFS | O(V + E) | O(V) | 경로 존재, 사이클 검출 |
| BFS | O(V + E) | O(V) | 최단 경로 (비가중) |
| 다익스트라 | O((V+E) log V) | O(V) | 최단 경로 (가중) |
| 위상 정렬 | O(V + E) | O(V) | 의존성 순서 결정 |

### Android 실전 활용

```kotlin
// 1. Navigation Graph - 화면 간 이동 경로
// nav_graph.xml이 내부적으로 방향 그래프
// <fragment id="homeFragment">
//     <action destination="detailFragment" />
//     <action destination="settingsFragment" />
// </fragment>

// 2. 위상 정렬 - Gradle 태스크 의존성
fun topologicalSort(graph: Map<String, List<String>>): List<String> {
    val inDegree = mutableMapOf<String, Int>()
    graph.keys.forEach { inDegree[it] = 0 }
    graph.values.flatten().forEach { inDegree[it] = (inDegree[it] ?: 0) + 1 }

    val queue = ArrayDeque<String>()
    inDegree.filter { it.value == 0 }.keys.forEach { queue.addLast(it) }

    val result = mutableListOf<String>()
    while (queue.isNotEmpty()) {
        val node = queue.removeFirst()
        result.add(node)
        graph[node]?.forEach { neighbor ->
            inDegree[neighbor] = inDegree[neighbor]!! - 1
            if (inDegree[neighbor] == 0) queue.addLast(neighbor)
        }
    }
    return result
}

// 3. Dagger/Hilt 의존성 그래프
// @Module, @Inject 관계가 DAG(유향 비순환 그래프)를 형성
// 순환 의존성이 있으면 컴파일 에러 발생

// 4. 소셜 그래프 - 친구 추천
fun suggestFriends(
    graph: Map<String, Set<String>>,
    user: String
): List<String> {
    val friends = graph[user] ?: return emptyList()
    val suggestions = mutableMapOf<String, Int>()

    friends.forEach { friend ->
        graph[friend]?.forEach { fof ->
            if (fof != user && fof !in friends) {
                suggestions[fof] = (suggestions[fof] ?: 0) + 1
            }
        }
    }
    return suggestions.entries
        .sortedByDescending { it.value }
        .map { it.key }
}
```

## 🔗 관련 개념

- [[00-CS-기초/자료구조/트리|트리]]
- [[00-CS-기초/자료구조/스택과-큐|스택과 큐]]
- [[00-CS-기초/자료구조/해시맵|해시맵]]
- [[00-CS-기초/알고리즘/탐색-알고리즘|탐색 알고리즘]]
- [[00-CS-기초/알고리즘/그리디|그리디]]

## 📚 더 보기

- [Android Navigation Graph](https://developer.android.com/guide/navigation/navigation-getting-started)
- [그래프 알고리즘 시각화](https://visualgo.net/en/dfsbfs)

---

**핵심 요약:** 그래프는 다대다 관계를 표현하며, Android의 Navigation Graph와 의존성 그래프가 대표적이고, BFS는 최단 경로, DFS는 경로 탐색에 사용된다.
