---
tags: [CS, 자료구조, 배열, 연결리스트, ArrayList, LinkedList]
---

# 배열 vs 연결리스트

## 💡 핵심 개념

배열(Array)은 연속된 메모리 공간에 데이터를 저장하는 자료구조이고, 연결리스트(Linked List)는 각 노드가 데이터와 다음 노드의 참조를 갖는 자료구조이다. 두 자료구조는 메모리 배치와 접근 방식에서 근본적인 차이를 가지며, 이에 따라 각 연산의 시간 복잡도가 달라진다.

## 📌 왜 필요한가?

Android에서 `RecyclerView`에 데이터를 바인딩할 때 `List`를 사용하며, 대부분 `ArrayList` 기반이다. 데이터의 접근 패턴(읽기 위주 vs 삽입/삭제 위주)에 따라 적절한 자료구조를 선택해야 성능을 최적화할 수 있다.

## 🔍 자세히

### 배열 (Array / ArrayList)

배열은 인덱스를 통한 **O(1) 랜덤 접근**이 가능하다. 메모리가 연속적이므로 CPU 캐시 히트율이 높다.

```kotlin
// 고정 크기 배열
val arr = IntArray(5) { it * 2 } // [0, 2, 4, 6, 8]
println(arr[3]) // O(1) 접근 → 6

// 동적 배열 (ArrayList)
val list = arrayListOf("A", "B", "C")
list.add("D")        // 끝에 추가: amortized O(1)
list.add(1, "X")     // 중간 삽입: O(n) - 뒤 요소 전부 이동
list.removeAt(0)     // 앞 삭제: O(n)
```

ArrayList는 내부적으로 배열이 가득 차면 **1.5배 크기**의 새 배열을 할당하고 복사한다.

### 연결리스트 (LinkedList)

각 노드가 다음(혹은 이전) 노드의 참조를 가진다. **삽입/삭제가 O(1)**이지만, 특정 위치 접근은 **O(n)**이다.

```kotlin
import java.util.LinkedList

val linked = LinkedList<String>()
linked.addFirst("A")   // O(1)
linked.addLast("C")    // O(1)
linked.add(1, "B")     // 탐색 O(n) + 삽입 O(1)

// Iterator를 통한 순회 중 삭제 - 안전하게 O(1)
val iter = linked.iterator()
while (iter.hasNext()) {
    if (iter.next() == "B") iter.remove()
}
```

### 시간 복잡도 비교

| 연산 | ArrayList | LinkedList |
|------|-----------|------------|
| 인덱스 접근 | **O(1)** | O(n) |
| 맨 끝 삽입 | amortized O(1) | **O(1)** |
| 맨 앞 삽입 | O(n) | **O(1)** |
| 중간 삽입 | O(n) | 탐색 O(n) + 삽입 O(1) |
| 검색 | O(n) | O(n) |
| 메모리 | 연속, 컴팩트 | 노드별 할당, 오버헤드 큼 |

### Android 실전 활용

```kotlin
// RecyclerView에는 ArrayList 기반 List가 적합
// → 인덱스 기반 접근이 빈번 (onBindViewHolder에서 list[position])
class MyAdapter(private val items: List<Item>) : RecyclerView.Adapter<VH>() {
    override fun onBindViewHolder(holder: VH, position: Int) {
        val item = items[position] // O(1) 접근 필수
        holder.bind(item)
    }
    override fun getItemCount() = items.size
}

// Kotlin의 불변 리스트 활용
val immutableList = listOf("A", "B", "C")        // 읽기 전용
val mutableList = mutableListOf("A", "B", "C")   // 수정 가능
```

### 선택 기준

- **읽기 위주 + 랜덤 접근** → `ArrayList` (대부분의 Android 시나리오)
- **빈번한 삽입/삭제 (특히 앞쪽)** → `LinkedList`
- **실무에서는 99% ArrayList를 사용**한다. LinkedList는 메모리 오버헤드와 캐시 미스 때문에 대부분의 경우 ArrayList보다 느리다.

## 🔗 관련 개념

- [[00-CS-기초/자료구조/스택과-큐|스택과 큐]]
- [[00-CS-기초/자료구조/해시맵|해시맵]]
- [[00-CS-기초/알고리즘/시간-복잡도-Big-O|시간 복잡도 Big-O]]
- [[00-CS-기초/메모리/스택-vs-힙|스택 vs 힙]]
- [[00-CS-기초/메모리/캐시-메모리|캐시 메모리]]

## 📚 더 보기

- [Kotlin Collections Overview](https://kotlinlang.org/docs/collections-overview.html)
- [ArrayList vs LinkedList - Baeldung](https://www.baeldung.com/java-arraylist-linkedlist)

---

**핵심 요약:** 랜덤 접근이 필요하면 ArrayList, 빈번한 삽입/삭제가 필요하면 LinkedList이지만, Android 실무에서는 거의 항상 ArrayList를 사용한다.
