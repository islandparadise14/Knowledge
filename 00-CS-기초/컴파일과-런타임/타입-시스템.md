---
tags: [CS, 타입시스템, Kotlin, 제네릭, 타입안전성]
---

# 타입 시스템

## 💡 핵심 개념

타입 시스템(Type System)은 프로그래밍 언어에서 각 값과 표현식에 타입을 부여하고, 타입 간의 연산 규칙을 정의하는 체계이다. 타입 시스템의 목적은 타입 관련 오류를 프로그램 실행 전에 감지하여 안정성을 높이는 것이다. Kotlin은 정적 타입, null 안전, 스마트 캐스트를 지원하는 강력한 타입 시스템을 갖추고 있다.

## 📌 왜 필요한가?

안드로이드 개발에서 Kotlin의 타입 시스템은 NPE 방지, API 계약 강제, IDE 자동완성 등 생산성과 안정성을 크게 향상시킨다. Java의 약점(null 안전성 부재, raw type 허용)을 Kotlin이 타입 시스템 수준에서 해결한다.

## 🔍 자세히

### 타입 시스템 분류

| 기준 | 분류 | 예시 |
|------|------|------|
| 검사 시점 | 정적 / 동적 | Kotlin(정적), Python(동적) |
| 엄격도 | 강타입 / 약타입 | Kotlin(강), JavaScript(약) |
| 추론 | 명시적 / 추론 | Java(명시적), Kotlin(추론 지원) |

### Kotlin 타입 계층 구조

```
         Any
        /   \
     Int   String   ...  (모든 non-null 타입)
        \   /
        Nothing        (모든 타입의 하위 타입, 값 없음)


         Any?
        /    \
     Int?   String?  ...  (모든 nullable 타입)
        \    /
        Nothing?
           |
         null           (Nothing?의 유일한 값)
```

### Null 안전성 (Null Safety)

```kotlin
// Kotlin의 타입 시스템은 null을 타입 레벨에서 구분
var name: String = "Android"    // non-null
var nickname: String? = null     // nullable

// 컴파일러가 null 접근을 차단
name.length        // OK
nickname.length    // 컴파일 에러!
nickname?.length   // OK - safe call
nickname!!.length  // 컴파일 OK, 런타임 NPE 위험

// 플랫폼 타입 (Java 코드에서 오는 타입)
// Java: public String getName() { ... }
val javaName: String = javaObj.name  // 플랫폼 타입 - NPE 가능
val safeName: String? = javaObj.name // 명시적 nullable로 안전 처리
```

### 스마트 캐스트 (Smart Cast)

```kotlin
fun process(value: Any) {
    // 타입 체크 후 자동으로 캐스트
    when (value) {
        is String -> println(value.length)    // 자동 String으로 캐스트
        is Int -> println(value + 1)          // 자동 Int로 캐스트
        is List<*> -> println(value.size)     // 자동 List로 캐스트
    }
}

// sealed class와 함께 사용하면 강력
sealed class ApiResponse<out T> {
    data class Success<T>(val data: T) : ApiResponse<T>()
    data class Error(val code: Int, val message: String) : ApiResponse<Nothing>()
    object Loading : ApiResponse<Nothing>()
}

fun <T> handleResponse(response: ApiResponse<T>) = when (response) {
    is ApiResponse.Success -> processData(response.data) // 스마트 캐스트
    is ApiResponse.Error -> showError(response.message)   // 스마트 캐스트
    is ApiResponse.Loading -> showLoading()
}
```

### 제네릭과 변성 (Variance)

```kotlin
// 공변성 (out) - 생산자: 타입을 반환만 함
interface Source<out T> {
    fun next(): T
}

// 반공변성 (in) - 소비자: 타입을 받기만 함
interface Sink<in T> {
    fun put(item: T)
}

// 실제 안드로이드 예시: LiveData는 공변적
// LiveData<out T> → LiveData<String>은 LiveData<Any>의 하위 타입
val stringLiveData: LiveData<String> = MutableLiveData("hello")
val anyLiveData: LiveData<Any> = stringLiveData // OK - 공변

// Comparable은 반공변적
// Comparable<in T>
val stringComparable: Comparable<String> = compareBy<Any> { it.hashCode() }
```

### 타입 별칭 (Type Alias)

```kotlin
// 복잡한 타입에 별칭 부여
typealias UserClickListener = (User) -> Unit
typealias NetworkResult<T> = Result<Pair<T, ResponseMetadata>>
typealias UserDao = Map<String, List<User>>

// 콜백 정의 시 가독성 향상
class UserAdapter(
    private val onClick: UserClickListener,
    private val onLongClick: UserClickListener
) : RecyclerView.Adapter<UserViewHolder>() {
    // ...
}
```

### value class로 타입 안전성 강화

```kotlin
@JvmInline
value class Email(val value: String) {
    init {
        require(value.contains("@")) { "유효하지 않은 이메일: $value" }
    }
}

@JvmInline
value class Password(val value: String) {
    init {
        require(value.length >= 8) { "비밀번호는 8자 이상이어야 합니다" }
    }
}

// 타입으로 실수 방지 - 컴파일 타임에 순서 바뀜 감지
fun login(email: Email, password: Password) { /* ... */ }

// login(Password("pass1234"), Email("a@b.com")) → 컴파일 에러!
```

## 🔗 관련 개념

- [[00-CS-기초/컴파일과-런타임/정적-vs-동적-타이핑|정적 vs 동적 타이핑]]
- [[00-CS-기초/컴파일과-런타임/타입-소거|타입 소거]]
- [[00-CS-기초/컴파일과-런타임/컴파일-타임-vs-런타임|컴파일 타임 vs 런타임]]
- [[00-CS-기초/소프트웨어-공학/OOP-객체지향|OOP 객체지향]]

## 📚 더 보기

- [Kotlin 타입 시스템 공식 문서](https://kotlinlang.org/docs/basic-types.html)
- [Kotlin Null Safety](https://kotlinlang.org/docs/null-safety.html)
- [Kotlin Generics](https://kotlinlang.org/docs/generics.html)

---

**핵심 요약:** Kotlin의 타입 시스템은 null 안전성, 스마트 캐스트, 제네릭 변성, value class 등을 통해 컴파일 타임에 다양한 오류를 방지하며, 안드로이드 앱의 안정성과 개발 생산성을 크게 향상시킨다.
