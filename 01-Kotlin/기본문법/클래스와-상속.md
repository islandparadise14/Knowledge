---
tags: [kotlin, basics, class, inheritance]
---

# 클래스와 상속

## 💡 핵심 개념

Kotlin의 클래스는 **기본적으로 final**이다. 상속을 허용하려면 `open` 키워드를 붙여야 한다. 이는 의도하지 않은 상속을 방지한다.

## 📌 왜 필요한가?

Java의 실수를 보완한 설계다. Effective Java에서도 "상속을 위한 설계와 문서를 갖추거나, 그럴 수 없다면 상속을 금지하라"고 권장한다. Kotlin은 이를 기본으로 적용했다.

## 🔍 자세히

### 기본 클래스

```kotlin
// 기본 클래스
class User {
    var name: String = ""
    var age: Int = 0
}

// 주 생성자 (Primary Constructor)
class User(val name: String, val age: Int)

// 부 생성자 (Secondary Constructor)
class User(val name: String) {
    var age: Int = 0
    
    constructor(name: String, age: Int) : this(name) {
        this.age = age
    }
}

// init 블록
class User(val name: String) {
    init {
        require(name.isNotBlank()) { "Name cannot be blank" }
        println("User created: $name")
    }
}
```

### 프로퍼티

```kotlin
class User(
    val name: String,  // 읽기 전용 프로퍼티
    var age: Int       // 가변 프로퍼티
) {
    // Custom getter/setter
    var email: String = ""
        get() = field.lowercase()
        set(value) {
            field = value.trim()
        }
    
    // Computed property (backing field 없음)
    val isAdult: Boolean
        get() = age >= 18
}
```

### 상속

```kotlin
// open 클래스 - 상속 가능
open class Animal(val name: String) {
    open fun sound() {
        println("Some sound")
    }
}

// 상속
class Dog(name: String) : Animal(name) {
    override fun sound() {
        println("Woof!")
    }
}

// final override - 더 이상 오버라이드 불가
open class Cat(name: String) : Animal(name) {
    final override fun sound() {
        println("Meow!")
    }
}
```

### 추상 클래스

```kotlin
abstract class Shape {
    abstract val area: Double
    abstract fun draw()
    
    // 구현된 메서드도 가능
    fun describe() {
        println("This is a shape with area $area")
    }
}

class Circle(val radius: Double) : Shape() {
    override val area: Double
        get() = Math.PI * radius * radius
    
    override fun draw() {
        println("Drawing circle")
    }
}
```

### 가시성 제어자

```kotlin
class MyClass {
    private val privateVar = 1        // 클래스 내부에서만
    protected val protectedVar = 2     // 클래스와 서브클래스
    internal val internalVar = 3       // 같은 모듈 내
    public val publicVar = 4           // 어디서나 (기본값)
}
```

### 주의사항

**생성자 호출 순서**

```kotlin
open class Parent {
    init {
        println("Parent init")
        initialize()  // 위험!
    }
    
    open fun initialize() {
        println("Parent initialize")
    }
}

class Child : Parent() {
    val property = "child"
    
    override fun initialize() {
        println("Child initialize: $property")  // null!
    }
}
// Parent init 시 Child의 property가 아직 초기화 안 됨
```

**해결책:** init에서 open 함수 호출하지 말 것

## 💻 실전 예제

### Android ViewModel 상속

```kotlin
abstract class BaseViewModel : ViewModel() {
    protected val _isLoading = MutableStateFlow(false)
    val isLoading: StateFlow<Boolean> = _isLoading.asStateFlow()
}

class UserViewModel(
    private val repository: UserRepository
) : BaseViewModel() {
    private val _users = MutableStateFlow<List<User>>(emptyList())
    val users: StateFlow<List<User>> = _users.asStateFlow()
    
    fun loadUsers() {
        viewModelScope.launch {
            _isLoading.value = true
            _users.value = repository.getUsers()
            _isLoading.value = false
        }
    }
}
```

## 🔗 관련 개념

- [[01-Kotlin/클래스-종류/Data-Class]]
- [[01-Kotlin/클래스-종류/Sealed-Class]]
- [[01-Kotlin/기본문법/인터페이스]]

## 📚 더 보기

- [Kotlin Docs - Classes](https://kotlinlang.org/docs/classes.html)

---

**핵심 요약:** 클래스는 기본 final, 상속하려면 open. init 블록에서 open 함수 호출 금지.
