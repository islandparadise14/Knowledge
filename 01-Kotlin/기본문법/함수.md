---
tags: [kotlin, basics, function]
---

# 함수

## 💡 핵심 개념

Kotlin의 함수는 **일급 객체(first-class citizen)**다. 함수를 변수에 할당하고, 인자로 전달하고, 반환할 수 있다.

### 일급 객체란?

프로그래밍 언어에서 **다른 값들과 동등하게 취급되는 대상**을 말한다.

**1. 변수에 할당 가능**
```kotlin
val sum: (Int, Int) -> Int = { a, b -> a + b }
val result = sum(3, 5)  // 8
```

**2. 함수의 인자로 전달 가능**
```kotlin
fun calculate(a: Int, b: Int, operation: (Int, Int) -> Int): Int {
    return operation(a, b)
}

calculate(10, 5) { x, y -> x + y }  // 15
```

**3. 함수의 반환값으로 사용 가능**
```kotlin
fun getOperation(type: String): (Int, Int) -> Int {
    return when (type) {
        "add" -> { a, b -> a + b }
        else -> { a, b -> a - b }
    }
}

val op = getOperation("add")
op(3, 4)  // 7
```

일급 객체이기 때문에 **함수형 프로그래밍**이 가능하며, 콜백/리스너 패턴을 간결하게 표현할 수 있다.

## 📌 왜 필요한가?

함수형 프로그래밍의 기초이며, 코드 재사용성과 가독성을 높인다. 안드로이드에서는 콜백, 리스너, 이벤트 처리에 필수다.

## 🔍 자세히

### 기본 사용법

```kotlin
// 기본 함수
fun sum(a: Int, b: Int): Int {
    return a + b
}

// 단일 표현식 함수
fun sum2(a: Int, b: Int) = a + b  // 타입 추론

// Unit 반환 (void와 유사, 생략 가능)
fun printSum(a: Int, b: Int) {
    println("Sum: ${a + b}")
}
```

### 파라미터

**기본 인자**

```kotlin
fun greet(name: String, message: String = "Hello") {
    println("$message, $name!")
}

greet("민트")  // Hello, 민트!
greet("민트", "안녕")  // 안녕, 민트!
```

**이름 있는 인자**

```kotlin
fun createUser(
    name: String,
    age: Int,
    city: String = "Seoul"
) { ... }

// 순서 바꿔서 호출 가능
createUser(
    age = 27,
    name = "민트"
)
```

**가변 인자 (vararg)**

```kotlin
fun sum(vararg numbers: Int): Int {
    return numbers.sum()
}

sum(1, 2, 3, 4, 5)

// spread 연산자
val nums = intArrayOf(1, 2, 3)
sum(*nums)  // *로 배열 펼치기
```

### 반환값

**Nothing 타입**

```kotlin
// 정상적으로 반환하지 않는 함수
fun fail(message: String): Nothing {
    throw IllegalStateException(message)
}

// 활용
val name = getName() ?: fail("Name is required")
```

**여러 값 반환**

```kotlin
// Pair 사용
fun getUserInfo(): Pair<String, Int> {
    return "민트" to 27
}

val (name, age) = getUserInfo()

// data class 사용 (권장)
data class UserInfo(val name: String, val age: Int)

fun getUserData(): UserInfo {
    return UserInfo("민트", 27)
}
```

### 확장 함수

```kotlin
// String에 함수 추가
fun String.isValidEmail(): Boolean {
    return this.contains("@")
}

"test@example.com".isValidEmail()  // true

// nullable 확장
fun String?.orEmpty(): String {
    return this ?: ""
}
```

### 동작 원리

**함수 타입**

```kotlin
// 함수 타입 선언
val sum: (Int, Int) -> Int = { a, b -> a + b }

// 함수 참조
fun add(a: Int, b: Int) = a + b
val sumRef: (Int, Int) -> Int = ::add
```

### 주의사항

**1. 기본 인자와 Java 호환성**

```kotlin
// @JvmOverloads로 오버로드 자동 생성
@JvmOverloads
fun greet(name: String, message: String = "Hello") {
    println("$message, $name")
}
```

**2. 꼬리 재귀 최적화**

```kotlin
// 일반 재귀 - 스택 오버플로 위험
fun factorial(n: Int): Long {
    return if (n == 1) 1 else n * factorial(n - 1)
}

// 꼬리 재귀 최적화
tailrec fun factorial2(n: Int, acc: Long = 1): Long {
    return if (n == 1) acc else factorial2(n - 1, n * acc)
}
```

## 💻 실전 예제

### Android ViewModel

```kotlin
class UserViewModel : ViewModel() {
    fun loadUser(
        userId: String,
        onSuccess: (User) -> Unit,
        onError: (Throwable) -> Unit
    ) {
        viewModelScope.launch {
            try {
                val user = repository.getUser(userId)
                onSuccess(user)
            } catch (e: Exception) {
                onError(e)
            }
        }
    }
}
```

### Extension functions for Android

```kotlin
// Context 확장
fun Context.showToast(message: String) {
    Toast.makeText(this, message, Toast.LENGTH_SHORT).show()
}

// Activity에서
showToast("Hello!")

// View 확장
fun View.show() {
    visibility = View.VISIBLE
}

fun View.hide() {
    visibility = View.GONE
}
```

## 🔗 관련 개념

- [[01-Kotlin/함수형-프로그래밍/고차함수]]
- [[01-Kotlin/함수형-프로그래밍/람다-표현식]]
- [[01-Kotlin/기본문법/확장-함수]]

## 📚 더 보기

- [Kotlin Docs - Functions](https://kotlinlang.org/docs/functions.html)

---

**핵심 요약:** 함수는 일급 객체. 기본 인자, 이름 인자, vararg 활용. 확장 함수로 기존 클래스에 기능 추가.
