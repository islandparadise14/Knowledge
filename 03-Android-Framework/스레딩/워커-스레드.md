---
tags: [android, thread, worker-thread, handler-thread, executor, 스레딩]
---

# 워커 스레드 (Worker Thread)

## 💡 핵심 개념

Android에서 **메인 스레드(UI 스레드)** 외에 백그라운드 작업을 수행하는 모든 스레드를 **워커 스레드**라 부른다. 네트워크 요청, DB 접근, 파일 I/O, 무거운 연산 등은 반드시 워커 스레드에서 실행해야 하며, 그렇지 않으면 ANR(Application Not Responding)이 발생한다..

## 📌 왜 필요한가?

Android의 메인 스레드는 UI 렌더링(16ms/프레임)과 사용자 입력 처리를 담당한다. 메인 스레드에서 5초 이상 블로킹하면 ANR 다이얼로그가 뜨고, 100ms만 블로킹해도 사용자가 버벅임을 느낀다. 워커 스레드의 종류와 특성을 알아야 상황에 맞는 선택을 할 수 있다.

## 🔍 자세히

### 메인 스레드 vs 워커 스레드

```
┌──────────────────────────────────────────────┐
│              Android 프로세스                  │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │  메인 스레드 (UI 스레드)                  │  │
│  │  - Looper + MessageQueue                 │  │
│  │  - UI 렌더링 (View.draw, layout)         │  │
│  │  - 입력 이벤트 처리 (터치, 키)            │  │
│  │  - Lifecycle 콜백 (onCreate 등)           │  │
│  │  - BroadcastReceiver.onReceive()         │  │
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 워커 #1  │ │ 워커 #2  │ │ 워커 #3  │ ...  │
│  │ 네트워크 │ │ DB 접근  │ │ 이미지   │      │
│  │ 요청     │ │          │ │ 디코딩   │      │
│  └──────────┘ └──────────┘ └──────────┘      │
└──────────────────────────────────────────────┘
```

```kotlin
// ❌ 메인 스레드에서 네트워크 → NetworkOnMainThreadException
override fun onCreate(savedInstanceState: Bundle?) {
    val data = URL("https://api.example.com/data").readText()  // 크래시!
}

// ❌ 메인 스레드에서 무거운 연산 → ANR
override fun onResume() {
    val result = (1..1_000_000).map { heavyComputation(it) }  // ANR
}
```

### 1. Thread + Handler (기본 패턴)

가장 원시적인 방법. 워커 스레드에서 작업 후 Handler로 메인 스레드에 결과 전달.

```kotlin
// 워커 스레드에서 작업 → Handler로 UI 업데이트
val handler = Handler(Looper.getMainLooper())

Thread {
    // 워커 스레드에서 실행
    val data = api.fetchData()  // 네트워크 요청

    handler.post {
        // 메인 스레드에서 실행
        textView.text = data
    }
}.start()

// ⚠️ 문제점:
// 1. 스레드를 무한정 생성할 수 있음 (리소스 낭비)
// 2. Activity 파괴 후에도 실행될 수 있음 (메모리 릭)
// 3. 취소 메커니즘 없음
// 4. 에러 처리 복잡
```

### 2. HandlerThread (전용 Looper 스레드)

자체 Looper를 가진 워커 스레드. 순차 실행이 보장되므로 동기화 없이 안전하다.

```kotlin
class SensorWorker {
    private val handlerThread = HandlerThread("sensor-worker").apply { start() }
    private val workerHandler = Handler(handlerThread.looper)
    private val mainHandler = Handler(Looper.getMainLooper())

    fun startCollecting() {
        workerHandler.post {
            // 센서 데이터 수집 (워커 스레드)
            val data = readSensor()

            mainHandler.post {
                // UI 업데이트 (메인 스레드)
                updateUI(data)
            }
        }
    }

    fun schedulePeriodicWork() {
        val runnable = object : Runnable {
            override fun run() {
                processSensorData()
                workerHandler.postDelayed(this, 1000)  // 1초마다 반복
            }
        }
        workerHandler.post(runnable)
    }

    fun destroy() {
        handlerThread.quitSafely()  // 반드시 종료
    }
}

// HandlerThread 특징:
// ✅ 순차 실행 보장 (단일 스레드 + MessageQueue)
// ✅ 동기화 불필요
// ⚠️ 병렬 처리 불가 (하나씩 순서대로)
// ⚠️ 반드시 quit() 또는 quitSafely() 호출
```

### 3. Executor / ThreadPoolExecutor

Java의 표준 스레드 풀. 스레드 재사용으로 생성 비용을 줄인다.

```kotlin
// 기본 Executor 종류
val singleThread = Executors.newSingleThreadExecutor()  // 순차 실행
val fixedPool = Executors.newFixedThreadPool(4)          // 4개 동시 실행
val cachedPool = Executors.newCachedThreadPool()         // 필요 시 생성, 유휴 시 회수
val scheduled = Executors.newScheduledThreadPoolExecutor(2) // 예약 실행

// 사용
fixedPool.execute {
    val data = api.fetchData()
    runOnUiThread { textView.text = data }
}

// Future로 결과 받기
val future: Future<String> = fixedPool.submit(Callable {
    api.fetchData()
})
val result = future.get()  // ⚠️ 블로킹! 메인 스레드에서 호출하면 안 됨

// 커스텀 ThreadPoolExecutor
val customPool = ThreadPoolExecutor(
    2,                      // corePoolSize: 기본 유지 스레드 수
    4,                      // maxPoolSize: 최대 스레드 수
    60L, TimeUnit.SECONDS,  // keepAliveTime: 유휴 스레드 대기 시간
    LinkedBlockingQueue(100) // 작업 큐 (최대 100개 대기)
)

// 종료
customPool.shutdown()       // 진행 중인 작업 완료 후 종료
customPool.shutdownNow()    // 즉시 종료 (인터럽트)
```

### 4. AsyncTask (Deprecated)

API 30에서 deprecated. 참고용으로만 알아두자.

```kotlin
// ❌ Deprecated - 사용 금지
class FetchTask : AsyncTask<String, Int, String>() {
    override fun doInBackground(vararg params: String?): String {
        return api.fetchData(params[0]!!)  // 워커 스레드
    }
    override fun onPostExecute(result: String) {
        textView.text = result  // 메인 스레드
    }
}

// 문제점:
// 1. Activity 수명주기를 인식하지 못함 → 메모리 릭
// 2. 구성 변경(회전) 시 결과 유실
// 3. 기본적으로 직렬 실행 (SERIAL_EXECUTOR)
// 4. 취소 시 doInBackground가 바로 멈추지 않음

// ✅ 대체: 코루틴
viewModelScope.launch {
    val result = withContext(Dispatchers.IO) { api.fetchData(url) }
    textView.text = result
}
```

### 5. IntentService (Deprecated)

API 30에서 deprecated. 워커 스레드에서 Intent를 순차 처리하는 Service.

```kotlin
// ❌ Deprecated
class UploadService : IntentService("upload") {
    override fun onHandleIntent(intent: Intent?) {
        // 워커 스레드에서 실행
        uploadFile(intent?.getStringExtra("path"))
    }
    // 모든 Intent 처리 후 자동 종료
}

// ✅ 대체: WorkManager + CoroutineWorker
class UploadWorker(ctx: Context, params: WorkerParameters)
    : CoroutineWorker(ctx, params) {
    override suspend fun doWork(): Result {
        val path = inputData.getString("path") ?: return Result.failure()
        uploadFile(path)
        return Result.success()
    }
}
```

### 6. 코루틴 Dispatcher (현재 권장)

```kotlin
// Dispatchers와 실제 스레드 매핑
Dispatchers.Main       // → 메인 스레드 (Handler 기반)
Dispatchers.IO         // → 공유 풀, 최대 64개 동시 실행
Dispatchers.Default    // → 공유 풀, CPU 코어 수만큼 동시 실행
Dispatchers.Unconfined // → 호출한 스레드에서 시작, 재개 시 변경 가능

// 워커 스레드 사용 예시
viewModelScope.launch {
    // 메인 스레드
    showLoading()

    val users = withContext(Dispatchers.IO) {
        // IO 워커 스레드
        userDao.getAll()
    }

    val sorted = withContext(Dispatchers.Default) {
        // CPU 워커 스레드
        users.sortedBy { it.name }
    }

    // 메인 스레드
    adapter.submitList(sorted)
    hideLoading()
}
```

### 워커 스레드 선택 가이드

```
작업 유형                          권장 방식
──────────────────────────────────────────────────────
단순 비동기 작업                  → 코루틴 (Dispatchers.IO / Default)
순차 백그라운드 처리              → 코루틴 (limitedParallelism(1))
지연 실행 / 반복 실행             → 코루틴 (delay / while 루프)
앱 종료 후에도 실행 보장          → WorkManager
실시간 통신 (센서, 오디오)        → HandlerThread (또는 코루틴)
레거시 Java 라이브러리 연동       → Executor + asCoroutineDispatcher()
Foreground 지속 작업              → Foreground Service + 코루틴
```

### 워커 스레드 공통 주의사항

```kotlin
// 1. UI 접근 금지
Thread {
    textView.text = "Hi"  // ❌ CalledFromWrongThreadException
}.start()

// 반드시 메인 스레드로 전환
Thread {
    val data = fetchData()
    runOnUiThread { textView.text = data }        // 방법 1
    view.post { textView.text = data }             // 방법 2
    handler.post { textView.text = data }          // 방법 3
    // 코루틴: withContext(Dispatchers.Main)        // 방법 4 (권장)
}.start()

// 2. Activity 참조 릭 방지
class MyActivity : AppCompatActivity() {
    // ❌ inner class가 Activity 참조를 잡고 있음
    inner class WorkerThread : Thread() {
        override fun run() {
            Thread.sleep(30_000)
            textView.text = "Done"  // Activity 파괴 후에도 참조 유지
        }
    }

    // ✅ viewModelScope은 ViewModel 수명에 맞춰 자동 취소
    // ✅ lifecycleScope은 Activity/Fragment 수명에 맞춰 자동 취소
}

// 3. 스레드 생성 비용 인식
// Thread 1개 = 1~2MB 스택 메모리
// 1000개 생성 → ~2GB 메모리 → OOM
// → 반드시 스레드 풀 또는 코루틴 사용
```

### Executor → Coroutine 브릿지

```kotlin
// 레거시 Executor 기반 코드를 코루틴과 함께 사용
val legacyExecutor = Executors.newFixedThreadPool(4)

// Executor를 Dispatcher로 변환
val dispatcher = legacyExecutor.asCoroutineDispatcher()

viewModelScope.launch {
    withContext(dispatcher) {
        // legacyExecutor의 스레드 풀에서 실행
        legacyLibrary.doWork()
    }
}

// 정리
dispatcher.close()
legacyExecutor.shutdown()
```

### @WorkerThread 어노테이션

```kotlin
// 메인 스레드에서 호출하면 안 되는 함수에 표시
// 런타임 체크가 아닌 Lint 경고용
@WorkerThread
fun fetchFromNetwork(): Data {
    return api.fetch()  // 네트워크 I/O
}

@MainThread
fun updateUI(data: Data) {
    textView.text = data.toString()
}

// Lint가 잘못된 스레드 호출을 경고해 줌
// 실제 런타임 에러는 StrictMode로 검출 가능
StrictMode.setThreadPolicy(
    StrictMode.ThreadPolicy.Builder()
        .detectNetwork()      // 메인 스레드 네트워크 감지
        .detectDiskReads()    // 메인 스레드 디스크 읽기 감지
        .penaltyLog()
        .build()
)
```

## 🔗 관련 개념

- [[02-Coroutines/기초/Dispatcher|Dispatcher]]
- [[02-Coroutines/내부-동작/Dispatchers-내부-구현|Dispatchers 내부 구현]]
- [[02-Coroutines/내부-동작/Handler-Coroutine-브릿지|Handler-Coroutine 브릿지]]
- [[00-CS-기초/동시성/프로세스-vs-스레드|프로세스 vs 스레드]]
- [[00-CS-기초/동시성/스레드-풀|스레드 풀]]
- [[07-Jetpack-Libraries/WorkManager/Worker|WorkManager Worker]]

## 📚 더 보기

- [Android Threading 가이드](https://developer.android.com/guide/background/threading)
- [Executor 공식 문서](https://developer.android.com/reference/java/util/concurrent/Executor)
- [StrictMode 문서](https://developer.android.com/reference/android/os/StrictMode)

---

**핵심 요약:** 워커 스레드는 메인 스레드 블로킹을 피하기 위한 백그라운드 스레드다. Thread, HandlerThread, Executor, AsyncTask(deprecated) 등이 있지만, 현재는 코루틴 Dispatcher가 권장된다. UI 접근은 반드시 메인 스레드에서, 스레드 직접 생성은 피하고 풀이나 코루틴을 사용하자.
